// Single on/off switch for "Bobby AI" — gates both the hero launch button
// (ChatBot.astro) and the API endpoint (pages/api/chat.ts). Flip to true and
// redeploy to bring it back; no other code changes needed.
export const CHAT_ENABLED = true;
